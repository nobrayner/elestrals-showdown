{
  "name": "elestrals-showdown",
  "version": "0.0.1",
  "description": "",
  "scripts": {
    "dev": "turbo run dev --parallel --no-cache",
    "test": "echo \"Warning: no tests\" && exit 0"
  },
  "devDependencies": {
    "turbo": "^1.5.5",
    "typescript": "^4.8.4"
  }
}
